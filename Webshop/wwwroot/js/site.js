// const guid = Cookies.get('guid');
// const cartUrl = new Request(`http://localhost:5002/api/cart/${guid}`);
//
// fetch(cartUrl)
//   .then(response => {
//   // We take the response Promise and return it as JSON.
//   return response.json();
// })
// .then(json => {
//   // Now we can now use the JSON as a normal object.
//   console.log(json);
// });
